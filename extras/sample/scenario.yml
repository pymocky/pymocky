mappings:
  - id: login
    request:
      url: .*pymock_login$
    response:
      body_json:
        success: true
  - id: login
    scenario: login-error
    request:
      url: .*pymock_login$
    response:
      body_json:
        success: false